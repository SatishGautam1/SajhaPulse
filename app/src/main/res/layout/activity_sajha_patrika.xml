<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_sajha_patrika.xml — Sajha Patrika News Feed Screen
    ═══════════════════════════════════════════════════════════
    • Crimson fixed AppBar (matches app-wide system bar theme)
    • RecyclerView of news cards fetched from Firebase /news/
    • ProgressBar shown during load, empty-state TextView on no data
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:fitsSystemWindows="false">

    <!-- Fixed crimson AppBar — identical pattern to content_main -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_patrika"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/toolbar_background"
        android:elevation="4dp"
        android:fitsSystemWindows="true">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar_patrika"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/toolbar_background"
            android:paddingEnd="4dp"
            app:contentInsetStart="0dp"
            app:contentInsetStartWithNavigation="0dp"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:navigationIconTint="@color/on_primary">

            <!-- Screen title -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingStart="4dp">

                <ImageView
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_marginEnd="8dp"
                    android:contentDescription="@null"
                    android:src="@drawable/ic_language"
                    app:tint="@color/on_primary" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:alpha="0.80"
                        android:fontFamily="@font/main_font_family"
                        android:text="साझा"
                        android:textColor="@color/on_primary"
                        android:textSize="10sp" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:fontFamily="@font/main_font_family"
                        android:text="Patrika"
                        android:textColor="@color/on_primary"
                        android:textFontWeight="700"
                        android:textSize="15sp" />
                </LinearLayout>
            </LinearLayout>
        </com.google.android.material.appbar.MaterialToolbar>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Content area -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="24dp"
            android:paddingTop="12dp">

            <!-- Loading indicator -->
            <ProgressBar
                android:id="@+id/progress_patrika"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="48dp"
                android:indeterminateTint="@color/md_theme_primary"
                android:visibility="gone" />

            <!-- Empty state -->
            <TextView
                android:id="@+id/tv_patrika_empty"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="80dp"
                android:fontFamily="@font/main_font_family"
                android:gravity="center"
                android:text="कुनै समाचार भेटिएन।\nNo articles found."
                android:textColor="@color/on_surface"
                android:textSize="14sp"
                android:visibility="gone" />

            <!-- RecyclerView for news articles -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rv_patrika"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipToPadding="false"
                android:nestedScrollingEnabled="false"
                android:paddingEnd="16dp"
                android:paddingStart="16dp" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>