<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_main.xml — ROOT shell only.
    DrawerLayout holds two children:
      [1] content_main  — full dashboard panel (first child = content)
      [2] nav_drawer    — sidebar panel (second child, gravity=start)

    Nothing else lives here. All UI is in the two included files.
    fitsSystemWindows="true" on DrawerLayout lets the system inset the
    status-bar area; the Toolbar inside content_main gets the padding.
-->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false"
    tools:context=".ui.MainActivity">

    <!-- PANEL 1 — Main dashboard (must be first child) -->
    <include
        layout="@layout/content_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- PANEL 2 — Navigation drawer (must be second child, gravity=start) -->
    <include
        layout="@layout/nav_drawer"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start" />

</androidx.drawerlayout.widget.DrawerLayout>