<?xml version="1.0" encoding="utf-8"?>
<!--
    item_quick_action.xml — Reusable Quick Action Item
    ═══════════════════════════════════════════════════════════════════
    Usage: <include layout="@layout/item_quick_action" android:id="@+id/qa_xxx" />
    Then in Java: qaView.setIcon(R.drawable.xxx); qaView.setLabel("Label");
    Or bind directly using the child view IDs below.
    ═══════════════════════════════════════════════════════════════════
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="4dp"
    android:clickable="true"
    android:focusable="true">

    <!-- Circular icon container -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/qa_icon_card"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:cardCornerRadius="30dp"
        app:cardBackgroundColor="@color/md_theme_primaryContainer"
        app:cardElevation="0dp"
        xmlns:app="http://schemas.android.com/apk/res-auto">

        <ImageView
            android:id="@+id/qa_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_services"
            android:contentDescription="@null"
            app:tint="@color/md_theme_primary" />

    </com.google.android.material.card.MaterialCardView>

    <!-- Label below icon -->
    <TextView
        android:id="@+id/qa_label"
        android:layout_width="72dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/placeholder_action"
        android:textSize="11sp"
        android:textColor="?attr/colorOnSurface"
        android:textAlignment="center"
        android:gravity="center"
        android:maxLines="2"
        android:lineSpacingMultiplier="1.2" />

</LinearLayout>