<?xml version="1.0" encoding="utf-8"?>
<!--
    nav_drawer.xml — SajhaPulse Side Navigation Panel
    ═══════════════════════════════════════════════════════════════════
    layout_gravity="start" MUST be on this root tag.
    If placed on the <include> in activity_main.xml the drawer will
    not open or will render incorrectly on some devices.

    fitsSystemWindows="true" lets NavigationView automatically inset
    its header below the status bar without extra Java code.
    ═══════════════════════════════════════════════════════════════════
-->
<com.google.android.material.navigation.NavigationView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_view"
    android:layout_width="300dp"
    android:layout_height="match_parent"
    android:layout_gravity="start"
    android:fitsSystemWindows="true"
    app:headerLayout="@layout/nav_header_main"
    app:menu="@menu/nav_drawer_menu" />